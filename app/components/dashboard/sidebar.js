import React from "../../../node_modules/react/react";
let Sidebar = React.createClass({

  render () {
    return(
      <div id="sidebar">
        No to derp
      </div>
    )
  }
});

export default Sidebar;
